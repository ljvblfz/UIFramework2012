<UserControl x:Class="ComponentArt.Silverlight.Demos.DemoList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
    DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <ScrollViewer x:Name="ItemsScrollViewer" Style="{StaticResource CArtScrollViewerStyle}" Background="Transparent" BorderBrush="Transparent"
                      VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Hidden">
            <StackPanel x:Name="ItemsStackPanel">
                <Image Margin="0,14,0,0"  Source="{Binding Header}" HorizontalAlignment="Left" VerticalAlignment="Top" Stretch="None" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>